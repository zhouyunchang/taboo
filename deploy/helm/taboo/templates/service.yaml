apiVersion: v1
kind: Service
metadata:
  name: {{ include "taboo.fullname" . }}
  labels:
    {{- include "taboo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taboo.selectorLabels" . | nindent 4 }}
