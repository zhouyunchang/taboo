{
  "name": "taboo-server",
  "version": "0.1.0",
  "description": "taboo (禁制) — lightweight self-hosted secrets management platform. MVP API server.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "smoke": "node scripts/smoke.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
